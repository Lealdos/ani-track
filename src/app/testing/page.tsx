import { Loader } from '@/shared/components/loading/Loader';

export default function Loading() {
    return (
        <div className='w-full h-full my-56'>
            <Loader/>
        </div>
    );
}
